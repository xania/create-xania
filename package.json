{
  "name": "create-xania",
  "description": "Reactive UI Library",
  "version": "0.0.0",
  "author": "Ibrahim ben Salah",
  "scripts": {
    "build": "tsup src/cli.ts --minify",
    "clean": "shx rm -rf lib",
    "dev": "tsup src/cli.ts --watch",
    "prepublishOnly": "npm run clean && npm run build",
    "test": "npx rimraf ../xania-app/* && node dist/cli.js ../xania-app"
  },
  "bin": "dist/cli.js",
  "files": [
    "dist",
    "templates"
  ],
  "devDependencies": {
    "rimraf": "^3.0.2",
    "@types/node": "^17.0.29",
    "tsup": "^5.12.1",
    "typescript": "^4.6.3"
  },
  "license": "MIT",
  "dependencies": {
    "create-create-app": "^7.3.0"
  }
}
