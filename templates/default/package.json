{
  "name": "{{ kebab name }}",
  "description": "{{ description }}",
  "version": "0.0.0",
  "author": "{{ author }}",
  "scripts": {
    "start": "npx vite",
    "build": "npx vite build --emptyOutDir"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "typescript": "^4.6.3"
  },
  "license": "MIT",
  "dependencies": {
    "@xania/view": "^0.4.7",
    "rxjs": "^7.5.7",
    "vite": "latest"
  }
}
