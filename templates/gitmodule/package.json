{
  "name": "{{ kebab name }}",
  "description": "{{ description }}",
  "version": "0.0.0",
  "author": "{{ author }}",
  "scripts": {
    "start": "npx vite",
    "build": "npx vite build --emptyOutDir"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "sass": "^1.56.1",
    "typescript": "^4.6.3"
  },
  "license": "MIT",
  "dependencies": {
    "prettier": "latest",
    "rxjs": "^7.5.7",
    "vite": "latest"
  }
}
